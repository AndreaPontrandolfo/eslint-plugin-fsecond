/** @type {import('eslint-doc-generator').GenerateOptions} */
const config = {
  ruleListColumns: [
    "name",
    "description",
    "configsError",
    "type",
    "options",
    "fixable",
    "hasSuggestions",
  ],
};

export default config;
