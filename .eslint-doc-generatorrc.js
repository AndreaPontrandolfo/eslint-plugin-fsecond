/** @type {import('eslint-doc-generator').GenerateOptions} */
const config = {
  ruleListColumns: [
    "name",
    "description",
    "configsError",
    "requiresTypeChecking",
    "options",
    "fixable",
    "hasSuggestions",
  ],
};

export default config;
