/** @type {import('eslint-doc-generator').GenerateOptions} */
const config = {
  ruleListColumns: [
    "name",
    "description",
    "configs",
    "type",
    "options",
    "fixable",
    "hasSuggestions",
  ],
};

export default config;
